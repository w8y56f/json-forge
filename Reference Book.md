# JSON Forge Reference Book

JSON Forge 是一个本地 JSON / JSON-like 编辑、格式化和查看工具。

## 常用功能

- 从混杂文本中提取首个完整 JSON 对象或数组。
- 格式化或压缩 JSON；支持注释、单引号、无引号键名和尾随逗号等 JSON5 风格输入。
- 切换键名为双引号、单引号或无引号；光标位置实时显示 JSONPath。
- 搜索内容，支持大小写、全词、选区、属性名和属性值范围。
- 括号匹配高亮、语法高亮、行号、代码折叠和结尾连接虚线。
- 多标签编辑：新建、关闭、拖动排序、重命名；每个标签可设置书签。
- 可切换浅色/深色主题、中文/English、标签样式、自动换行和退出确认；会自动恢复上次会话。

## 快捷操作

- `Ctrl/Cmd + Enter`：格式化
- `Ctrl/Cmd + Shift + M`：压缩 JSON
- `Ctrl/Cmd + T`：新建标签
- `Ctrl/Cmd + W`：关闭当前标签
- `Ctrl/Cmd + F`：搜索
- `Ctrl/Cmd + 鼠标滚轮`：缩放编辑器字体
- `Ctrl/Cmd + +` / `-` / `0`：放大 / 缩小 / 重置字体
- `Ctrl/Cmd + F2`：添加或移除当前行书签
- `F2` / `Shift + F2`：跳转到下一处 / 上一处书签
